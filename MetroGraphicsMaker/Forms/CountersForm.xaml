<Window x:Class="Forms.CountersForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CountersForm" Height="300" Width="500">
    <Grid x:Name="mainTableLayout">
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="230"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ScrollViewer Width="450" Height="auto" Background="Aquamarine">
            <DataGrid x:Name="CountersDataGrid" AutoGenerateColumns="False" SelectionMode="Extended" ItemsSource="{Binding}" SelectionUnit="FullRow" Width="420" Height="220" BorderThickness="2" FontFamily="Times New Roman Bold" HorizontalContentAlignment="Center" >
                <DataGrid.Columns>
                    <DataGridTextColumn x:Name="FirstColumn" Header="Останавливаемся?" Width="130"></DataGridTextColumn>
                    <DataGridTextColumn x:Name="SecondColum" Header="Значение счёта" Width="140"></DataGridTextColumn>
                    <DataGridTextColumn x:Name="ThirdColumn" Header="Значение Остановки" Width="150"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
        <Button x:Name="OkButton" Grid.Row="1" Content="OK" Width="100" Height="30" HorizontalAlignment="Right" Margin="45,0" Background="LightSkyBlue" BorderThickness="2" ></Button>-->
    </Grid>
</Window>
